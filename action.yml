name: Squiddy's first workflow!
description: Write some text
inputs:
  who-to-greet:
    description: 'Who to greet'
    required: true
    default: 'World'
  github_event:
    required: true
outputs:
  time:
    description: 'The time we greeted you'
runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - ${{ inputs.who-to-greet }}
    - ${{ inputs.github_event }}
